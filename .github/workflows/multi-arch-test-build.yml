name: Feeds Package Test Build

on:
  pull_request:

permissions:
  contents: read
  checks: read

jobs:
  # TEST-ONLY: wait-for-formalities gate removed for fork-local CI validation.
  # This branch/workflow change is throwaway and must never be merged upstream.
  build:
    name: Feeds Package Test Build
    uses: openwrt/actions-shared-workflows/.github/workflows/multi-arch-test-build.yml@main

